# APM dependencies
apm_modules/

# Python
.venv/
.venv-live/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Ephemeral run outputs
results/
artifacts/
# Explicit: red-team raw transcripts must never leave the workspace (already
# covered by the results/ ignore above; path called out for audit-trail
# clarity per Council conditions C6/C7).
results/redteam/

# Committed hermetic test fixtures (must survive the results/ and artifacts/ ignores above)
!tests/fixtures/
!tests/fixtures/**

# Local environment / secrets
.env
config/azure.env
*.local

# OS / editor
.DS_Store
Thumbs.db

# Python build artifacts
build/
*.egg-info/

